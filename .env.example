# Required
TELEGRAM_BOT_TOKEN=
TELEGRAM_USER_ID=

# Optional — only needed if Claude Code CLI is not already authenticated
# If you've run `claude` on this server, it already has auth and this is not needed
ANTHROPIC_API_KEY=

# Optional (defaults shown)
MAX_TURNS=25
WORKING_DIRECTORY=~
